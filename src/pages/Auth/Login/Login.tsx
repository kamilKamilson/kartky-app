import Button from '../../../components/atoms/Button/Button';

const Login = () => {
  return (
    <div>
      <h2>Log in</h2>
      <Button isFull>Log in</Button>
    </div>
  );
};

export default Login;
